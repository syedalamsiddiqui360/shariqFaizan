@model IEnumerable<shariqFaizan.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ct_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ct_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ct_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ct_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.ct_id }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.ct_id })
        </td>
    </tr>
}

</table>
